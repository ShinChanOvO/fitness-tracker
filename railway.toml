[build]
builder = "nixpacks"
buildCommand = "npm install && npm rebuild sqlite3"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartDelaySecs = 10

[variables]
PORT = "3000"
NODE_VERSION = "20"
